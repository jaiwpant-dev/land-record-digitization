/* Chart.js hook; app.js provides a CSS fallback if the CDN is unavailable. */
document.addEventListener('DOMContentLoaded',()=>{if(!window.Chart)return;const bars=document.querySelector('.bars');if(!bars)return;bars.innerHTML='<canvas id="digitizationChart"></canvas>';new Chart(document.getElementById('digitizationChart'),{type:'bar',data:{labels:['Apr','May','Jun','Jul','Aug','Sep'],datasets:[{label:'Records digitized',data:[9100,12400,15800,19300,24000,21820],backgroundColor:'#1769aa',borderRadius:6}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false}},scales:{y:{beginAtZero:true}}}});});
